version: "3.8"

services:
  app:
    image: reaksa7236/ets-evaluat-v2:dev-1.0.2
    container_name: laravel12-app
    restart: unless-stopped
    environment:
      PORT: 8080
      APP_NAME: Laravel
      APP_ENV: local
      APP_KEY: base64:nKy3bfQf/STDcTlOExN2BnIqXr9k3VHgV0jTKB3Wna4=
      APP_DEBUG: true
      APP_URL: http://localhost

      DB_CONNECTION: pgsql
      DB_HOST: host.docker.internal
      DB_PORT: 5432
      DB_DATABASE: T01
      DB_USERNAME: pguser_dev
      DB_PASSWORD: 2tZxHFizMpxll5
    volumes:
        - .:/var/www/html
        - /var/www/html/vendor
    networks:
      - laravel-net

  nginx:
    image: nginx:stable-alpine
    container_name: laravel12-nginx
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
      - ./docker/nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:
      - laravel-net


networks:
  laravel-net:
